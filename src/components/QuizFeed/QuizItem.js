import React from 'react';
import styled from 'styled-components';

const SQuizItem = styled.div``

const QuizItem = ({ title }) => {
  return (<SQuizItem>{title}</SQuizItem>);
}

export default QuizItem;