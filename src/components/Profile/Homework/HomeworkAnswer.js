import React from 'react';

const HomeworkAnswer = ({ type }) => {
  console.log(type);
  return (<React.Fragment>
    {/* {type === "obj"} */}

  </React.Fragment>);
}

export default HomeworkAnswer;